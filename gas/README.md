# Google Apps Script 連携ガイド

このガイドでは、Google スプレッドシートから直接評価システムを実行できるようにする手順を説明します。

## 📋 目次

1. [Google Apps Script の設定](#google-apps-script-の設定)
2. [使い方](#使い方)
3. [トラブルシューティング](#トラブルシューティング)

---

## 🔧 Google Apps Script の設定

### ステップ1: スプレッドシートを開く

1. 生徒マスタースプレッドシートを開く
   - URL: https://docs.google.com/spreadsheets/d/YOUR_SPREADSHEET_ID

### ステップ2: スクリプトエディタを開く

1. メニューバーから **拡張機能** > **Apps Script** をクリック
2. 新しいタブでスクリプトエディタが開きます

### ステップ3: スクリプトをコピー

1. `Code.gs` ファイルの内容を全て削除
2. `/gas/Code.gs` の内容をコピー&ペースト
3. **保存**（Ctrl+S / Cmd+S）

### ステップ4: トリガーを設定（オプション）

カスタムメニューを自動で表示するため、トリガーを設定します：

1. スクリプトエディタで **時計アイコン（トリガー）** をクリック
2. 右下の **＋トリガーを追加** をクリック
3. 以下の設定を行う：
   - 実行する関数: `onOpen`
   - イベントのソース: `スプレッドシートから`
   - イベントの種類: `起動時`
4. **保存** をクリック

### ステップ5: 初回実行

1. スクリプトエディタで `onOpen` 関数を選択
2. **実行** ボタンをクリック
3. 権限の承認を求められたら、以下の手順を実行：
   - **権限を確認** をクリック
   - Googleアカウントを選択
   - **詳細** をクリック
   - **（安全ではないページ）に移動** をクリック
   - **許可** をクリック

### ステップ6: 確認

1. スプレッドシートに戻る
2. ページを再読み込み（F5）
3. メニューバーに **📊 評価実行** メニューが表示されることを確認

---

## 📖 使い方

### 評価を実行する

1. スプレッドシートを開く
2. メニューバーから **📊 評価実行** > **🚀 評価を実行** をクリック
3. ダイアログで以下を入力：
   - **評価対象月**: 評価したい月を選択（例: 2024-12）
   - **生徒ID**（オプション）:
     - 特定の生徒のみ評価する場合、1行に1つのIDを入力
     - 空欄の場合は全生徒を評価
4. **🚀 評価を実行** ボタンをクリック
5. 評価完了まで待つ（数分かかる場合があります）
6. 完了通知が表示されたら、結果スプレッドシートを確認

### 評価結果の確認

評価結果は以下のスプレッドシートに自動で書き込まれます：

- **シート名**: `評価結果_YYYY-MM`（例: 評価結果_2024-12）
- **列構成**:
  - A列: 評価月
  - B列: 学籍番号
  - C列: 氏名
  - D列: 欠席評価
  - E列: 遅刻評価
  - F列: ミッション評価
  - G列: 支払い評価
  - H列: アクティブリスニング評価
  - I列: 理解度評価
  - J列: 総合評価
  - K列: コメント
  - L列: 評価日時

---

## 🔍 トラブルシューティング

### メニューが表示されない

**原因**: スクリプトが正しく設定されていない、または権限が不足している

**解決方法**:
1. スクリプトエディタを開く
2. `onOpen` 関数を実行して、権限を承認
3. スプレッドシートを再読み込み

### 「API returned status 500」エラー

**原因**: バックエンドAPIでエラーが発生している

**解決方法**:
1. Renderダッシュボード（https://dashboard.render.com/）を開く
2. サービスのログを確認
3. エラー内容に応じて対応

### 「評価の実行に失敗しました」エラー

**原因**: ネットワークエラー、または入力値が不正

**解決方法**:
1. 入力値を確認（評価対象月、生徒ID）
2. API URL（`https://vtuber-school-evaluation.onrender.com/api/evaluate`）が正しいか確認
3. しばらく待ってから再実行

### 評価に時間がかかる

**原因**: 複数の生徒を評価している、またはGemini APIの応答が遅い

**対策**:
- 少数の生徒ずつ評価する（5-10名程度）
- ピーク時間を避ける
- 完了通知が表示されるまで待つ（ダイアログを閉じない）

---

## 🆘 サポート

問題が解決しない場合は、以下の情報を添えて開発者に連絡してください：

- エラーメッセージの全文
- 実行したタイミング（日時）
- 評価対象の生徒ID
- スクリプトエディタのログ（表示 > ログ）
